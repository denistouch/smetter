SELECT laureates.firstname, laureates.surname, prizes.year, prizes.category, affiliations.country
FROM laureates,
     prizes,
     affiliations
WHERE laureates.id = prizes.laureate_id
  AND laureates.id = affiliations.laureate_id
  AND year LIKE '%'
  AND category LIKE '%'
  AND country LIKE '%'
GROUP BY laureates.id